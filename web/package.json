{
  "name": "claudeskill-cn-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro",
    "gen:og": "node scripts/gen-og.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=claudeskill-cn"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.5.0",
    "mermaid": "^11.15.0",
    "pagefind": "^1.5.2"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
